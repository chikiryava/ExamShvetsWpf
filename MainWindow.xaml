<Window x:Class="ExamShvets.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExamShvets"
        xmlns:local1="clr-namespace:ExamShvets.ViewModels"
        Icon="./images/Мастер пол.png"
        mc:Ignorable="d"
        FontFamily="Segoe UI"
        Title="dsds" Height="450" Width="800">
    <Window.DataContext>
        <local1:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Image Source="/images/Мастер пол.png" Grid.Row="0" Height="50" VerticalAlignment="Top" HorizontalAlignment="Left" />

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="10" Grid.Row="1">
        
        <ItemsControl ItemsSource="{Binding Partners, Mode=OneWay}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical"  />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            
            <ItemsControl.ItemTemplate>
                
                <DataTemplate>
                    <Border BorderBrush="LightGray" BorderThickness="1" 
                  CornerRadius="2" Margin="0,0,0,10" Padding="10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Orientation="Vertical">
                                <TextBlock FontSize="16" FontWeight="SemiBold">
                  <Run Text="{Binding Type}"/>
                  <Run Text=" | "/>
                  <Run Text="{Binding Name}"/>
                                </TextBlock>


                                <TextBlock Margin="0,4,0,0">
                                    <Run Text="Директор"/>
                                    <Run Text="{Binding Position}"/>
                                </TextBlock>
                                <TextBlock Text="{Binding Phone}" />
                                <TextBlock>
                  <Run Text="Рейтинг: "/>
                  <Run Text="{Binding Rating}"/>
                                </TextBlock>
                            </StackPanel>

                            <TextBlock Grid.Column="1"
                         Text="{Binding Discount, StringFormat={}{0}%}"
                         VerticalAlignment="Center"
                         FontSize="16" FontWeight="SemiBold"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
    </Grid>
</Window>
